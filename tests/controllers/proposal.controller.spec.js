const assert = require('assert');
const proposalController = require('../../../src/api/controllers/proposal.controller');

const expect = require('chai').expect;
const should = require('chai').should();